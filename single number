#include <stdio.h>

int main() {
    int N, num, result = 0;
    scanf("%d", &N);
    
    for(int i = 0; i < N; i++) {
        scanf("%d", &num);
        result ^= num;
    }
    
    printf("%d", result);
    
    return 0;
}
